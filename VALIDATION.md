# Validation

- Status: Executable application-bootstrap validations

## Validation Source of Truth

This document owns stable validation names for this scaffold.

## Standard Validation Names

| Name | Owner | Current contract |
|---|---|---|
| `format` | Oxfmt write mode | Formats only approved project-owned paths. |
| `format-check` | Oxfmt check mode | Fails when approved project-owned paths are not formatted. |
| `lint` | Oxlint and `oxlint-tsgolint@7` | Runs correctness, import, and type-aware rules without replacing the authoritative type gate. |
| `typecheck` | TypeScript 7 `tsc --noEmit` | Authoritative TypeScript diagnostics. |
| `test` | Vitest 4 | Runs one-item/two-skill loadout normalization, one-slot map pickup replacement, two opposite treasure ships with one flight per ship, three-to-seven-tile inland gift targeting, landing-only item creation, six mana-backed combat skills, the obstacle-only cooldown Grappling Hook with bare-ground rejection and sustained tree/Brick pulling, pure definition-driven action targeting and HUD view models, Soap owner immunity plus delayed damage and follow-up stun, proportional Bomb owner damage, Chain Bind and Arc Bolt forward-cone assistance with obstacle-aware targeting, persistent skill knockback, full-path bot Dodge safety, bounded tree/Brick detours, stalled-bot recovery, target commitment, one-claimant map-item pursuit, line-of-sight skill selection, pressure-aware Brick Bag and Soap decisions, health/mana regeneration, damage, shields, control and zone state, Boat land rejection and automatic generated-water boarding, direct six-trait spending and per-trait caps, retained skill-rank scaling compatibility, six-stat command normalization, replay v8 setup and hashes, report v11 selection and combat history, fixed public settings, deterministic island/lake/tree/diagonal-coast/opening-collapse/coast-sector-artillery rules, sixteen-mask terrain-autotile composition, movement/combat/item effects, round sealing, scoreboard persistence, keyboard/pointer/gamepad input state including bounded held-stick aim repeat, single-submit Pixi targeting presentation, presentation boundaries, deterministic varied bot loadouts, and targeting coverage. |
| `contract` | Repository-owned Bun TypeScript checks | Validates module boundaries, forbidden dependencies, the least-privilege Clarissimi workflow shape, asset provenance, the absence of development-only telemetry markup from public HTML, and other repository contracts that Oxlint cannot express reliably. |
| `migration-check` | Not applicable until a persisted format or compatible migration surface exists | Must report `NOT_APPLICABLE`, not fake success. |
| `smoke` | Playwright Test | Proves Chrome boot, menu/scoreboard/history flows, hidden pre-game canvas, one-item/two-skill settings with six icon-backed detailed skill cards and horizontal decrease/value/increase attribute steppers including hold-to-repeat and five-step quick buttons on every device with a desktop smoke asserting the 5-step buttons stay visible and the two-column trait cards never clip their steppers, compressed mobile loadout cards whose longest effect stays under half the screen including extra-large text, with a 320×568 extra-large smoke asserting the longest item and skill cards stay within half the viewport, phone-width loadout cards in two columns from 376 px with tightened copy that never clips, a coarse-pointer settings screen whose sticky save/cancel bar stays in the viewport while the form scrolls, touch-sized volume sliders on coarse-pointer screens, tablet-width loadout cards in two columns without text clipping, a landscape main menu that fits every action without vertical scrolling, a landscape settings smoke asserting the last skill and item cards stay fully visible at the bottom of the form without overlapping the side-by-side save bar, a landscape extra-large pause smoke asserting the four-column statistics keep the resume action on the first screen without horizontal overflow, a foldable cover-width smoke at 260 px asserting the menu, all four settings tabs, the attribute steppers, and the arena controls stay inside the viewport without horizontal overflow, a pre-round briefing that fits the coarse-pointer portrait and landscape viewports without internal scrolling or document overflow including at extra-large text, DEV-only diagnostics whose tuning contract omits removed shove controls, WebGL initialization, full-viewport arena ownership without document scrolling or text selection, player-follow camera movement through arrows/mouse/touch joystick with countdown warmup and capture-loss cleanup, post-elimination and completed-round camera panning through arrows and pointer drag, rejected WASD movement, keyboard and arena-owned pointer `Q/W` skill aiming, reusable `E` Grappling Hook aiming, `D` item aiming, touch action bridging with state-mirroring touch buttons, a fixed-size coarse-pointer joystick with two-by-two touch action buttons on narrow phones, a touch-sized top-right coarse-pointer pause trigger clear of the controls, a coarse-pointer combat readout that collapses to a one-line summary, expands on tap, stays clear of the touch controls in both portrait and landscape, and never clips at any text size, an expanded combat stat panel that stays clear of the joystick on narrow phones, whose large-text cells widen to two columns without clipping, and a width-capped stat toggle that stays at least 8 px clear of the pause trigger, with a 390×844 smoke journey booting a real round and asserting the toggle and pause trigger do not overlap, a 320×568 journey that keeps the joystick, touch actions, stat toggle, pause trigger, and expanded stat panel clear of one another, a 320×568 completed-round layout smoke that keeps the action buttons touch-sized with the statistics reachable by scrolling, a version-history layout that keeps the current entry open while past entries collapse to one-line summaries with a working expand action and a show-more smoke that opens with 30 entries, reveals the full history on demand, and never overflows the viewport, a desktop completed-round smoke asserting three-column actions and four-column statistics fit the panel without scrolling, a desktop paused-panel smoke asserting the folded control guide and three-column pause actions with four-column statistics fit the panel without scrolling, a narrow fine-pointer smoke asserting the pause trigger stays in the top corner clear of the joystick and touch actions with the stat panel folded by default, P pause persistence with elapsed time, rank, eliminations, land, movement, combat, control, skill-hit/use, and item-use statistics including shared compact coarse-pointer pause cells without horizontal overflow and a tighter pause layout that keeps the first statistics row on the first screen, content-sized phone trait cards that keep four-line mobility effects fully visible, six direct kill-reward trait choices with a visible save action, restart/report flows, sound fallback, reduced motion, fatal recovery, and WebGL restoration. |
| `smoke-dist` | Vite production build plus Playwright Test | Runs one tagged production critical journey against `vite preview` of the generated `dist` artifact: hidden pre-game canvas, valid saved loadout, briefing confirmation, generated terrain and effect assets, a live arena, action HUD readiness, and zero developer-panel/debug-tuning nodes. The broader local `smoke` suite retains interaction and failure-path coverage outside the two-minute hosted gate. |
| `capture-submission` | Clean exact-HEAD production build plus Playwright Chrome | Refuses tracked worktree drift, records deterministic 1920×1080 menu and fixed-70 Bomb-plus-Grappling-Hook gameplay screenshots, a short WebM, browser diagnostics, version identifiers, and SHA-256 checksums under the ignored `.cache/submission-captures/<full-sha>/` boundary. Passing `main` and manual CI runs upload that exact-SHA bundle for 30 days when capture succeeds; capture failure remains separate from the already-tested Pages artifact. Pull-request merge refs do not create submission media. |
| `profile-scale` | Bun hard-difficulty headless local profile | Measures 7,200 ticks at the public 70-participant setting with deterministic Brick Bag, two-Bomb, and Grappling Hook activity, separating AI and simulation duration while reporting wall, Bomb, and Hook samples, simultaneous detonations, candidate/full-pair source counts, long steps, real-time throughput, and observational heap delta. Hook-use counts remain descriptive rather than a merge gate; the simulation p95 gate is 10 ms, and AI timing remains visible independently because it is staggered rather than charged every simulation tick. |
| `audit-rounds` | Bun deterministic round, balance, and pacing audit | Runs sixteen fixed-seed rounds at the public 70-participant setting, plus smaller controlled diagnostic rotations for base mass, item grants, and collapse speed. Production shard schema v11 records duration, terminal results, item spawn bands, active-item uses by definition, Bomb-owner self-deaths, observational exposure, strategy results, equal-slot win rates, no-item-relative controlled rankings, 95% Wilson intervals, buff/nerf investigation signals, controlled item winner distribution, and collapse-speed duration distributions. It fails on malformed results, time-limit draws, a missing outer-ring preference, extreme controlled mass/item skew, or loss of the declared collapse pacing relationship. Ranking signals remain descriptive review inputs rather than merge gates, and the audit is not human-play or risky-pickup balance evidence. |
| `audit:items` | Bun deterministic starting-loadout balance audit | Runs at least sixty-four fixed-seed 8-participant rounds with Fast collapse, a 45-second audit limit, and production item spawns disabled. The sample count grows to keep at least sixteen actor-round slots per treatment as the auditable catalog expands. Normal-mass scripted actors rotate independently through bot personalities, the no-item control, every implemented passive starting item, and every unique passive pair. It reports wins, 95% Wilson intervals, no-item-relative win indices, credited eliminations, mean survival time, pair independence expectations, synergy indices, duration bounds, and time-limit seeds without dumping every round record. Charged active items stay excluded until bot-use policy and comparable use opportunities exist. It fails only on malformed/incomplete sampling; buff and nerf labels are review signals rather than automated balance verdicts. |
| `profile-browser` | Qualified production build plus local headless Chrome | Builds first, then requires five 500 ms aggregate-host-CPU samples to stay at or below 35% average and 65% maximum before Chrome starts. A rejected preflight is environment evidence, not a product failure. An accepted run measures fixed hard-difficulty Brick-plus-Bomb and Grappling-Hook-plus-Bomb workloads at 1280×720 after a separate startup warmup and same-arena stabilization. Both conditions report frame p95, long frames, requested-versus-delivered ticks, backlog, effective DPR, and forced-GC heap change after 20 restarts. The Hook condition spends both charges before its result is accepted. It is not cross-browser or field evidence. |
| `audit:focus` | Bun focused-trait balance audit | Runs eighty deterministic Slow-collapse 60-participant controlled rounds through four to eight isolated worker processes. Every round assigns exactly ten actors to each of the six `20/0/0/0/0/0` focused trait builds. Every trait receives the same schedule of all 21 two-skill combinations, four starting items, and five bot personalities while actor seats rotate independently. Map item spawning stays disabled so starting-item replacement cannot confound the trait comparison. The report records average rank, Top 10/5, win rate with descriptive actor-slot Wilson intervals, survival, credited eliminations, damage, skill use/hit rates, death causes, and round duration. Results are fixed-seed hard-bot evidence for extreme builds, not one-point marginal effects or human-play proof; same-round participants are dependent and time-limit rounds make winner-only rates sparse. |
| `audit:random` | Bun random-trait all-loadout balance audit | Runs eighty deterministic Slow-collapse 60-participant controlled rounds through four to eight isolated worker processes. Each actor receives a randomized 20-point six-trait build; six cyclic rotations of every sampled build keep each trait at exactly 200 invested points per round and 16,000 points overall. All 15 two-skill combinations and four starting items appear exactly once per round, with personalities and actor seats rotated independently. Map item spawning stays disabled. Trait rows are point-weighted associations rather than isolated one-point causal effects; skill, combination, item, and personality rows remain actor-round comparisons. |
| `docs` | Repository and ssealed documentation checks | Validates source-of-truth links, critical-document scaffold removal, static release and rollback contracts, submission and asset-provenance surfaces, and Markdown hygiene. |
| `build` | Vite 8 | Produces the provider-neutral static `dist` artifact with relative asset URLs that remain valid at the GitHub Pages `/shovefall/` project path. |
| `audit:strategy` | Bun TypeScript | Runs fixed all-bot rounds at the public 70-participant setting and reports personality wins, credited eliminations, survival time, overlapping item exposure, mass exposure, round duration, and time-limit counts. This focused repository script is evidence for the aggression-versus-survival decision, not a human fairness claim. |
| `check` | Aggregate command | Runs the configured merge-blocking validations without silently skipping a missing command. |

## Required Final Report

Final responses must list executed validations, passed validations, skipped validations, skip reasons, and remaining risk.

## Runner Policy

Ssealed runner remains `none`; package scripts are the application command surface rather than a generated task-runner file. Agents must use the configured `shovefall_*` mustflow intents. Unconfigured commands must fail, not pass with fake success.

## Hosted CI Boundary

GitHub Actions runs `check` and `smoke-dist` on `ubuntu-24.04` with Bun `1.3.14` and the committed lockfile. The workflow is source-owned at `.github/workflows/ci.yml`. Pull requests receive only `contents: read` and never upload or deploy an artifact. Successful `main` and manual runs attempt a clean exact-SHA submission-media bundle and upload it separately for 30 days when capture succeeds. Capture failure remains visible and skips only that optional upload; it does not block upload or deployment of the exact `dist` directory already exercised by `smoke-dist`. The tested build artifact passes to a separate least-privilege GitHub Pages deployment job with only `contents: read`, `pages: write`, and `id-token: write`. `.github/workflows/clarissimi.yml` owns a separate read-only advisory PR gate plus scoped post-merge draft and manual promotion jobs; neither workflow makes a hosted result merge enforcement until branch protection requires its stable check. Local success is not hosted evidence, a hosted green validation is not deployment evidence, and either result is not merge enforcement until branch protection requires it.

## Hygiene Validation

Repository hygiene file changes must check line-ending churn, binary diff pollution,
tracked secret files, ignored build/cache artifacts, and generated-output drift.

## Scope

Frontend validations use the accepted stack in `docs/engineering/08-toolchain-baseline.md`. Exact package patch versions are owned by `package.json` and `bun.lock`.

## Repository Shape

Web-app validation covers the playable DOM/PixiJS gray-box, a 57×48 deterministic connected public island with exactly twelve separated 5–9-tile lakes and cannon-driven collapse through the final stable tile, one public 70-participant Hard-AI mode, six starting attributes, two selected mana-backed skills, one built-in Grappling Hook, one starting item and one public inventory slot, fixed opening Slow collapse, eight initial map items, two alternating treasure ships yielding one launch about every 3.5 seconds with three-to-seven-tile inland targeting, no public round time limit, direct six-trait kill rewards, health/mana resources and control state, sixteen coast-sector pirate ships, one closest-ship projectile per accepted warned tile on independent per-ship 1.5-to-2.25-second intervals, omission of unreachable coast candidates instead of unbacked warnings, no ammunition counter or terminal rock phase, three item risk bands, the six-skill catalog, active items, obstacles, bounded water support, pre-round countdown, combat/progression HUD state, round result, fresh-world restart, optional procedural effects and licensed same-origin looping background music with independent persisted levels, reduced-motion presentation, renderer loss recovery, spatial broad phase, local production-artifact smoke, deterministic mass, synthetic internal-tier screens, and the 70-participant local headless profile without claiming hosted deployment, physical-device, cross-browser, audio-device, combat/item balance, risky-pickup balance, human balance, or external playtest approval.

## Formatting Boundary

Oxfmt must ignore `.agents/**`, `.ssealed/**`, dependencies, caches, coverage, and build output. Existing seeded documents are not bulk-formatted merely to satisfy a new formatter. Markdown prose wrapping remains preserved unless a document owner explicitly changes it.
